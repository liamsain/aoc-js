import { getAdventOfCodeData, logTime } from '../node-utils.js';
const input = await getAdventOfCodeData(2017, 1);
const start = performance.now();


const end = performance.now();
logTime(end - start);
    